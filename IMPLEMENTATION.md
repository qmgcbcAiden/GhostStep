# 预测式人机共驾开发版

本分支以 `bf5c518620400547468518aa794eae556724aedc` 为基础，实现第一版可测试的有限动作预测控制。此处描述已实现行为；仓库原有 ANALYSIS.md / PLAN_TIER1.md 是旧方案，不能用其中的权重、方向锁和耗时承诺解释此版本。

## 使用

1. 将安装包中的 `ghoststep3` 文件夹放入游戏 mods 目录；先备份原版本，避免同时启用同名的创意工坊版本和开发版。
2. MCM → GhostStep3 → 常规 → 辅助强度：**低 / 适中 / 高**，默认适中。已有存档的档位序号保留，但名称和含义已按新需求调整；修改即时生效并保存。
3. 左 Alt 切换保护，关闭后不改写移动，换房不会自动开启。再次开启清理旧决策。射击和边沿触发动作保持透传。近战、献祭、主动承伤时用 Alt 关闭。
4. 独立文件录制需要游戏启动参数 `--luadebug`；否则保留内存回放。日志位于 mod 内 `recordings/`。MCM 录制页显示队列、丢弃及错误状态。
5. 调试时可先在 MCM → 录制开启事件诊断，再用控制台 `gs mark` 标记不舒服的走位；`gs status` 看状态，`gs replay` 导出轻量回放到控制台和游戏日志。

## 已实现

- 控制器评估最终的移动输入，按玩家更新后的采样时序，先积分当前速度，再计算输入影响的下一速度，考虑近似加速、惯性和制动，优先比较全力度持续撤离，再比较全力度移动后恢复原输入和制动。安全时保持原输入；有危险时，只选择完整评估且风险下降的候选。选中后不再叠加未经检查的方向混合。
- 小幅输入保留力度；零输入能真正覆盖移动轴以制动。原方向变化不再受到永久方向锁影响。始终读取新硬件输入，避免辅助反馈进原始输入。
- 地形使用线性索引、CollisionClass/GetType、玩家圆形足迹和真实房间边界；每 3 个引擎帧刷新，飞行/危险格开关变化立即刷新。受阻或局部无解时触发有节点/时间上限的局部搜索；路点仍须通过预测检查。
- 圆体、激光、炸弹和动画前兆共用生效窗口与几何检查。圆体在时间段内检测相对运动，激光做保守扫掠；修正历史环形缓冲、激光重复旋转和动画方向被当成移动速度的问题。
- 动画条目有规则、置信度、生效时间、方向依据及结束时间；同源、同期实际攻击替换前兆，并记录匹配时差。此匹配属于诊断线索，不等同于精确的攻击事件识别。
- 炸弹优先读取 ExplosionCountdown，包括玩家自己的炸弹；未知引信明确标记并保守处理。飞行忽略地面 creep，关闭 creep 检测不再把冲击波等其他效果一并关闭。
- 弹体上限使用风险堆筛选，遍历后保留更可能接近玩家的对象；记录 omittedCount。规划有候选数、检查次数和时间预算，采用包围盒排除无关路径并复用预测。密集圆形弹幕会缩短窗口；大范围攻击保留较长窗口。搜索中断不会把未完成候选当成安全动作。
- 伤害回调正确接收 mod 首参。pre_damage 回调记为 damage_attempt，HP 下降记为 hit；保护关闭的掉血不计入辅助失败。诊断不再以低速直接认定卡死，也不把未知原因强行标成反应太慢。

## 采集数据与性能语义

Schema 2 保存构建指纹、配置、地形关键帧、引擎 frame、逻辑 tick/decisionId、实际输入、获选命令、是否启用、输入 hook 观测和下一次运动反馈。轻量时间线包含模型/预算摘要；详细事件包含候选及危险源几何、历史、生效时间和截断数。

日常默认保留轻量记录；MCM 开启事件诊断后才预采集详细缓存（开启之前的详细数据无法补录）：前 60、后 30 个引擎帧，最多约 2 MiB；重叠窗口合并，每次更新最多导出两条上下文。前置数据是实际预采集，超限丢弃明确计数。普通队列最多约 1 MiB，每次写入约 32 KiB，关键事件保留一部分队列空间。写入或 flush 返回失败会停止重试并保留队列；文件末尾可能不完整，不声称成功。

`budgetMs` 是本次规划耗时。`perfPrevious` 是前一次更新的完整耗时，包含感知、地形、规划、编码和写盘，并带自身 tick；渲染及其他 mod 开销不包含在内。Isaac.GetTime 的记录精度为整数毫秒，零值参与平均，缺失值不参与。字节/工作量上限不能保证操作系统写盘永远在固定时间内返回。

工具兼容旧附件与 Schema 2：

```sh
python tools/replay_viewer.py --help
python tools/diagnose.py path/to/session.jsonl --output diagnosis.json
```

诊断工具合并事件详细快照，检查低位移片段、保护关闭后发出命令、数据缺口和预算；低位移也可能是主动制动，需要结合反馈判断。离线结果不等于反事实通关率。

## 验证与实机边界

```sh
# Python 需安装 lupa（同时包含 lua51 / lua53）
python tests/run_smoke.py
python -m unittest discover -s tests -p 'test_*.py'
python tests/benchmark.py
python tools/package_mod.py --output ../dist/GhostStep-predictive-dev.zip
```

离线覆盖基础模块、历史反例、新控制行为和 main.lua 回调接线。CPU 探针是宿主机 Lupa 的合成场景，不能当成以撒帧率或超级撒旦实测。

仍需实机调校：角色真实加减速与引擎帧单位、擦弹余量、墙格碰撞细节、特殊激光/爆炸免疫、动画数据库误差及其他输入 mod 的交互。在线模型只对可用运动片段做受限拟合；它是近似模型，未经实机证明为准确。当前没有形式化 CBF 安全证明、完整强化学习策略或自动学习攻击库。

多人和多实体角色仍沿用不介入守卫。密集且无可行空间时可能找不到能改善风险的动作，日志会标明；本版本不承诺站立无伤。默认“适中”是本轮调优起点，还不是实测最优结果。

## 正常闪避修复（2026-09-09）

取消小位移目标：删除 0.3 / 0.6 候选、半速输入和行程惩罚。非零避让方向使用正常力度；制动仍可作为经过完整检查的备选。安全时恢复原操作，Alt 与射击输入不变。MCM 强度仍控制提前量、裕量和玩家意图偏好。

附件回放中 67,622 条决策记录有 14,070 条预算内无改进，1,647 次接管包含 770 次停止和 451 次半速。正常撤离候选曾排在末尾，而最多只完成 15 个评估（含基线）。现在先验证持续全力度动作，不可执行的地形路径提前淘汰，复用端点碰撞检查，停止与局部搜索放到后面。三级日志增加选中动作力度和预测持续时长，便于辨别实际接管类型。

36,837 对有运动的相邻记录中，34,608 对位置增量与上一帧速度相差不足 0.01；修正了原先提前积分新速度的错误。回归测试包含附件真实位置/速度数据、只有两个候选名额的搜索和修正时序的闭环避弹。爆炸用例的可达时间由 16 改为 17 帧：加入这一帧延迟后，原来的 16 帧从静止逃出测试半径并不可达。

附件没有 build 字段，不能证明用户安装包与某个提交逐字一致。测试在 Lua 模拟环境运行，仍需游戏内验证碰撞、其他模组和重度 Boss 弹幕。预算不足仍不使用未完成校验的路径，也不承诺无伤。

## 爆炸残骸与录制卡顿修复（2026-09-09）

TNT 的 GridEntity、State 和 VarData 可能在爆炸后保留；不能仅据这些字段把格子重新标为障碍。现在以当前碰撞值为前提，无碰撞的残骸不再产生 TNT 危险标记。附件中有 40 条格子样本同时出现 `danger=tnt` 和 `collision=0`（并非 40 个不同炸药桶）。回归复现旧行为失败后，验证销毁、连续刷新、飞行以及站在残骸处不会触发空场逃生。实体历史本身仍保留，但主决策只接收当前帧实体，未观察到的炸弹不会因历史缓存继续参与规划。

用户确认问题是整幅画面掉帧。已有回放的 recordingMs 有 692 帧超过 33 ms，最大 87 ms；decisionMs 最大 4 ms，terrainMs 最大 2 ms。首次载入另有一次 2119 ms 传感器阶段异常，不能归入日常写盘问题。旧日志没有细分编码和 I/O，所以录制阶段峰值不能全部确定为磁盘耗时。

帧内文件输出现在使用缓冲 write，不再每帧强制 flush。单次写入超过 8 ms 时暂停本局后续文件输出，保留有界待写队列与内存回放；队列满后计数丢弃，MCM 录制状态显示暂停。用户重新开关录制或开始新会话恢复。退出/关闭录制时尝试写完队列。同步 I/O 的首次阻塞无法被 Lua 中断，关闭文件也可能等待；意外退出可能丢失尚未刷出的尾部。

新增 perfPrevious.writerMs / recordPrepareMs、recordIoPaused / recordMaxWriteMs，区分下次复测的写盘与快照准备峰值。测试模拟一次 45 ms 写盘后连续 100 次写入请求，确认不再执行磁盘写入、队列仍有界；正常帧不调用 flush，显式 flush 和失败诊断保留。未把离线模拟当成实机掉帧已消除的证明。

## 新回放的可观测性修复（2026-09-09 晚）

新附件明确记录 frame 14263 的 write 调用 64 ms、writerMs 65 ms、totalMs 67 ms；当时无威胁，因此可以确认这一帧发生同步写盘阻塞。另一个 3200 ms sensorsMs 不能认定为传感器：旧计时从进入更新开始，包含 startRecording。现在新增 lifecycleMs 单独记录录制启动等前置操作；优先直接打开文件，只在失败时尝试创建目录，避免目录已存在仍每局启动 shell。未将整个 3.2 秒归因于 mkdir。

附件有 678 条连续快照，最后在 frame 14893，死亡事件却在 20382。暂停写盘后旧队列保留开局并丢弃后续快照，造成后半局诊断盲区。现在暂停时以有界队列滚动保留最新快照，优先保留关键事件；关闭文件时按原 seq 合并写出。仍会出现明确的序号缺口，不能把保留窗口当成整局。受伤回调增加有大小限制的 damage_context，携带上一决策的候选与近场危险几何，并明确标记采样阶段，避免把旧决策状态冒充伤害瞬间。

有完整决策数据的 654 条记录里，118 次接管均为全力度且 hook 被观测到；617 对运动记录的位置变化均符合上一帧速度。后续事件仍有预算内无改进和降低暴露后受伤，缺少当时完整几何，故本次不凭这些摘要调整规划权重或扩大预算。

构建信息改为模块装配时加载；若加载失败，会在 session_start 写 buildError，不再静默缺失。开局重置录制 tick 与上一局性能记录。分析工具增加序号缺口、快照窗口和事件计数。

回归包括慢写后滚动保留最新快照与关键事件、导出顺序、目录存在时不调用 shell、模拟 1200 ms 启动不计入传感器、受伤上下文关联。同步 I/O 和退出时写盘仍可能阻塞，未承诺全程不卡顿或无伤。
